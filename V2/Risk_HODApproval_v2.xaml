<Grid
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="766.605"
    d:DesignWidth="1200"
    Background="#FFEFF1F1"
    Name="Risk_HODApproval_v2"
    >

  <!-- Root layout:
         Row 0 = header
         Row 1 = main content -->
  <Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="*"/>
  </Grid.RowDefinitions>

  <!-- =========================
         HEADER
         ========================= -->
  <Border Grid.Row="0" Background="#8B82B6" Margin="4" CornerRadius="4">
    <Grid Margin="8,6">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <Label Grid.Column="0"
             Content="HOD Approval Area for Matter Risk Assessments"
             FontWeight="Bold"
             Foreground="White"
             FontSize="16"
             VerticalAlignment="Center"
             HorizontalAlignment="Left"/>

      <StackPanel Grid.Column="1"
                  x:Name="stk_CurrentUser"
                  Orientation="Horizontal"
                  VerticalAlignment="Center">
        <TextBlock Text="Current User:"
                   Foreground="White"
                   VerticalAlignment="Center"/>
        <ComboBox x:Name="cbo_User"
                  Width="215"
                  Height="26"
                  Margin="4,0,0,0">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Code, Mode=TwoWay}" Width="38" />
                <TextBlock Text="{Binding Name, Mode=TwoWay}" />
                <TextBlock Text="{Binding Email, Mode=TwoWay}" Visibility="Collapsed" />
              </StackPanel>
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>
        <Label x:Name="lbl_UserStatus"
               Content="-UserStatus-"
               Foreground="Gray"
               Visibility="Collapsed"/>
      </StackPanel>
    </Grid>
  </Border>

  <!-- =========================
         MAIN CONTENT
         ========================= -->
  <Grid x:Name="grd_Main"
          Grid.Row="1"
          Margin="8"
          MinHeight="0"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="320" MinWidth="240" MaxWidth="900" />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" MinWidth="400" />
    </Grid.ColumnDefinitions>

    <GridSplitter Grid.Column="1"
                  Width="6"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Stretch"
                  Background="Silver"
                  ShowsPreview="True"
                  ToolTip="Click and drag me to view more or less of one particular side">
      <GridSplitter.Template>
        <ControlTemplate TargetType="{x:Type GridSplitter}">
          <Grid>
            <Button Content="⁞" FontWeight="Bold"/>
            <Rectangle Fill="#00FFFFFF" />
          </Grid>
        </ControlTemplate>
      </GridSplitter.Template>
    </GridSplitter>

    <!-- =========================
             LEFT PANEL
             ========================= -->
    <Border Grid.Column="0"
            CornerRadius="8"
            BorderBrush="Gray"
            BorderThickness="1"
            Margin="0,0,4,0"
            Padding="4"
            MinHeight="0">

      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Content="Fee Earners in my Department"
               Foreground="Gray"
               FontWeight="Bold"
               HorizontalContentAlignment="Center" />

        <TextBlock Grid.Row="1"
                   x:Name="tb_NoFEs"
                   Text="No MRA's to approve or Current User is not an Approval user"
                   Foreground="Red"
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   Margin="0,0,0,6"/>

        <DataGrid Grid.Row="2"
                  x:Name="dg_FeeEarners"
                  AlternatingRowBackground="#FFAEDEB9"
                  SelectionMode="Single"
                  AutoGenerateColumns="False"
                  IsTextSearchEnabled="True"
                  ClipboardCopyMode="IncludeHeader"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  RowDetailsVisibilityMode="Collapsed"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True"
                  Background="#FFF0F0F0" BorderBrush="Gray">

          <DataGrid.Resources>
            <Style TargetType="{x:Type DataGridRow}">
              <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF0078D7" />
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF0078D7" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White" />
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White" />
              </Style.Resources>
            </Style>

            <Style x:Key="GroupHeaderStyle" TargetType="{x:Type GroupItem}">
              <Setter Property="Margin" Value="0,2,0,4"/>
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type GroupItem}">
                    <Expander x:Name="exp"
                              IsExpanded="True"
                              Background="LightGray"
                              BorderBrush="DarkGray"
                              BorderThickness="1"
                              Foreground="Black"
                              Padding="2"
                              Margin="2,0">
                      <Expander.Header>
                        <DockPanel>
                          <TextBlock FontWeight="Bold" Text="{Binding Path=Name}" Margin="5,0,0,0" />
                          <TextBlock Text=" (" FontWeight="Bold" />
                          <TextBlock FontWeight="Bold" Text="{Binding Path=ItemCount}"/>
                          <TextBlock Text=")" FontWeight="Bold" />
                        </DockPanel>
                      </Expander.Header>
                      <ItemsPresenter />
                    </Expander>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>

              <Style.Triggers>
                <DataTrigger Binding="{Binding Path=Name}" Value="01) Main">
                  <Setter Property="Template">
                    <Setter.Value>
                      <ControlTemplate TargetType="{x:Type GroupItem}">
                        <Expander x:Name="expander"
                                  IsExpanded="True"
                                  Background="#FF58B940"
                                  BorderBrush="#FF306323"
                                  BorderThickness="1"
                                  Foreground="Black"
                                  Padding="2"
                                  Margin="2,0">
                          <Expander.Header>
                            <DockPanel>
                              <TextBlock FontWeight="Bold" Text="{Binding Path=Name}" Margin="5,0,0,0" />
                              <TextBlock Text=" (" FontWeight="Bold" />
                              <TextBlock FontWeight="Bold" Text="{Binding Path=ItemCount}"/>
                              <TextBlock Text=")" FontWeight="Bold" />
                            </DockPanel>
                          </Expander.Header>
                          <ItemsPresenter />
                        </Expander>
                      </ControlTemplate>
                    </Setter.Value>
                  </Setter>
                </DataTrigger>

                <DataTrigger Binding="{Binding Path=Name}" Value="02) Holiday Cover">
                  <Setter Property="Template">
                    <Setter.Value>
                      <ControlTemplate TargetType="{x:Type GroupItem}">
                        <Expander x:Name="expander"
                                  IsExpanded="True"
                                  Background="#FFEBB91A"
                                  BorderBrush="#FF795D02"
                                  BorderThickness="1"
                                  Foreground="Black"
                                  Padding="2"
                                  Margin="2,0">
                          <Expander.Header>
                            <DockPanel>
                              <TextBlock FontWeight="Bold" Text="{Binding Path=Name}" Margin="5,0,0,0" />
                              <TextBlock Text=" (" FontWeight="Bold" />
                              <TextBlock FontWeight="Bold" Text="{Binding Path=ItemCount}"/>
                              <TextBlock Text=")" FontWeight="Bold" />
                            </DockPanel>
                          </Expander.Header>
                          <ItemsPresenter />
                        </Expander>
                      </ControlTemplate>
                    </Setter.Value>
                  </Setter>
                </DataTrigger>
              </Style.Triggers>
            </Style>

            <Style x:Key="RightAligned" TargetType="{x:Type DataGridCell}">
              <Setter Property="HorizontalAlignment" Value="Right" />
            </Style>

            <Style x:Key="CellCentre" TargetType="{x:Type DataGridCell}">
              <Setter Property="HorizontalAlignment" Value="Center" />
            </Style>

            <Style x:Key="Center" TargetType="{x:Type DataGridColumnHeader}">
              <Setter Property="ContentTemplate">
                <Setter.Value>
                  <DataTemplate>
                    <TextBlock TextWrapping="Wrap"
                               Text="{Binding}"
                               TextAlignment="Center"
                               Height="38" />
                  </DataTemplate>
                </Setter.Value>
              </Setter>
              <Setter Property="HorizontalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="DataGridContentCellCentering" TargetType="{x:Type DataGridCell}">
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type DataGridCell}">
                    <Grid Background="{TemplateBinding Background}">
                      <ContentPresenter VerticalAlignment="Center" />
                    </Grid>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>

            <Style x:Key="DGCentre" TargetType="DataGridCell">
              <Setter Property="TextBlock.TextAlignment" Value="Center" />
            </Style>
          </DataGrid.Resources>

          <DataGrid.GroupStyle>
            <GroupStyle ContainerStyle="{StaticResource GroupHeaderStyle}">
              <GroupStyle.Panel>
                <ItemsPanelTemplate>
                  <DataGridRowsPresenter />
                </ItemsPanelTemplate>
              </GroupStyle.Panel>
            </GroupStyle>
          </DataGrid.GroupStyle>

          <DataGrid.CellStyle>
            <Style TargetType="{x:Type DataGridCell}">
              <Style.Triggers>
                <DataTrigger Binding="{Binding feOrder}" Value="0">
                  <Setter Property="FontWeight" Value="Bold" />
                </DataTrigger>
                <DataTrigger Binding="{Binding feColour}" Value="Red">
                  <Setter Property="Foreground" Value="Red" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </DataGrid.CellStyle>

          <DataGrid.Columns>
            <DataGridTextColumn Header="Fee Earner"
                                IsReadOnly="True"
                                Width="Auto"
                                Binding="{Binding feName}"
                                Visibility="Collapsed" />

            <DataGridTextColumn Header="Branch"
                                IsReadOnly="True"
                                Binding="{Binding feBranch}"
                                Visibility="Hidden" />

            <DataGridTextColumn Header="Our Ref"
                                IsReadOnly="True"
                                Width="Auto"
                                Binding="{Binding ourRef}" />

            <DataGridTextColumn Header="Client Name"
                                IsReadOnly="True"
                                Width="100"
                                Binding="{Binding clName}">
              <DataGridTextColumn.ElementStyle>
                <Style TargetType="TextBlock">
                  <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                </Style>
              </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="Matter Desc"
                                IsReadOnly="True"
                                Width="120"
                                Binding="{Binding matDesc}">
              <DataGridTextColumn.ElementStyle>
                <Style TargetType="TextBlock">
                  <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                </Style>
              </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="MRA Name"
                                IsReadOnly="True"
                                Width="Auto"
                                Binding="{Binding mraName}">
              <DataGridTextColumn.ElementStyle>
                <Style TargetType="TextBlock">
                  <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                </Style>
              </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="Expiry Date"
                                IsReadOnly="True"
                                Width="Auto"
                                Binding="{Binding mraExpiryDate, StringFormat=dd/MM/yyyy}"
                                CellStyle="{StaticResource DGCentre}" />

            <DataGridTextColumn Header="OV_ID"
                                IsReadOnly="True"
                                Binding="{Binding ov_ID}"
                                Visibility="Collapsed" />

            <DataGridTextColumn Header="EntRef"
                                IsReadOnly="True"
                                Binding="{Binding entRef}"
                                Visibility="Hidden" />

            <DataGridTextColumn Header="MatNo"
                                IsReadOnly="True"
                                Binding="{Binding matNo}"
                                Visibility="Hidden" />

            <DataGridTextColumn Header="Approval Type"
                                IsReadOnly="True"
                                Binding="{Binding AType}"
                                Visibility="Hidden" />
          </DataGrid.Columns>
        </DataGrid>
      </Grid>
    </Border>

    <!-- =========================
             RIGHT PANEL
             ========================= -->
    <Border Grid.Column="2"
            CornerRadius="8"
            BorderBrush="Gray"
            BorderThickness="1"
            Margin="4,0,0,0"
            Padding="4"
            MinHeight="0">

      <Grid MinHeight="0">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- top title / action row -->
        <Grid Grid.Row="0" Margin="0,0,0,4">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>

          <Label Grid.Column="0"
                 Content="Selected Fee Earners' Matter Risk Assessment"
                 Foreground="Gray"
                 FontWeight="Bold"
                 HorizontalContentAlignment="Center"
                 HorizontalAlignment="Center"/>

          <Button Grid.Column="1"
                  x:Name="btn_ApproveHR"
                  Content="Approve"
                  ToolTip="Clicking this button will unlock the selected matter, and marks this item as 'Approved by HOD'..."
                  Margin="0,0,10,0"
                  Width="80" 
                  Background="LightGreen"
                  BorderBrush="DarkGreen"/>
        </Grid>

        <!-- no data message -->
        <StackPanel Grid.Row="1"
                    x:Name="stk_SelectedMRAHeader_NoData"
                    Visibility="Collapsed"
                    Orientation="Horizontal"
                    Margin="4,0,0,6">
          <TextBlock Foreground="Red"
                     FontWeight="Bold"
                     Text="Please select a Matter Risk Assessment from the list on the left."
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Padding="6,0,0,0"/>
        </StackPanel>

        <!-- selected header -->
        <Grid Grid.Row="1"
              x:Name="stk_SelectedMRAHeader"
              Visibility="Visible"
              Margin="4,0,0,6">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0" Orientation="Vertical">
            <TextBlock Text="Our Ref:" Foreground="Gray" VerticalAlignment="Center" TextAlignment="Right"/>
            <TextBlock Text="MRA Name:" Foreground="Gray" VerticalAlignment="Center" TextAlignment="Right"/>
          </StackPanel>

          <StackPanel Grid.Column="1" Orientation="Vertical" Margin="4,0,0,0">
            <TextBlock x:Name="tb_OurRef" Foreground="#FF6150BE" VerticalAlignment="Center" Text="[Our Ref]" />
            <TextBlock x:Name="tb_MRA_Name" Foreground="#FF6150BE" VerticalAlignment="Center" Text="[MRA Name]"/>
          </StackPanel>

          <TextBlock Grid.Column="2"
                     x:Name="tb_MRA_ID"
                     Visibility="Hidden"
                     Width="30"
                     Margin="0"/>

          <StackPanel Grid.Column="3" Orientation="Vertical">
            <TextBlock Text="Client Name:" Foreground="Gray" VerticalAlignment="Center" TextAlignment="Right"/>
            <TextBlock Text="Matter Desc:" Foreground="Gray" VerticalAlignment="Center" TextAlignment="Right"/>
          </StackPanel>

          <StackPanel Grid.Column="4" Orientation="Vertical" Margin="4,0,0,0" MaxWidth="260">
            <TextBlock x:Name="tb_ClName" Foreground="#FF6150BE" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="[Client Name]"/>
            <TextBlock x:Name="tb_MatDesc" Foreground="#FF6150BE" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="[Matter Description]"/>
          </StackPanel>

          <StackPanel Grid.Column="6" Orientation="Vertical" Margin="20,0,0,0">
            <TextBlock Text="Fee Earner:" Foreground="Gray" VerticalAlignment="Center" TextAlignment="Right"/>
            <TextBlock Text="FE Email:" Foreground="Gray" VerticalAlignment="Center" TextAlignment="Right"/>
          </StackPanel>

          <StackPanel Grid.Column="7" Orientation="Vertical" Margin="4,0,0,0">
            <TextBlock x:Name="tb_FE_Name" Foreground="#FF6150BE" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="[FE Name]" />
            <TextBlock x:Name="tb_FE_Email" Foreground="#FF6150BE" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="[FE Email]"/>
          </StackPanel>
        </Grid>

        <!-- main review grid -->
        <DataGrid Grid.Row="2"
                  x:Name="dg_MRAReview"
                  AlternatingRowBackground="#FFAEDEB9"
                  SelectionMode="Single"
                  AutoGenerateColumns="False"
                  IsTextSearchEnabled="True"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True"
                  BorderBrush="Gray" Background="#FFF0F0F0" >

          <DataGrid.Resources>
            <Style TargetType="{x:Type DataGridRow}">
              <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF0078D7" />
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF0078D7" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White" />
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White" />
              </Style.Resources>
            </Style>

            <Style x:Key="RightAligned" TargetType="DataGridCell">
              <Setter Property="HorizontalAlignment" Value="Right"/>
            </Style>

            <Style x:Key="Center" TargetType="DataGridCell">
              <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="CenterCol" TargetType="DataGridColumnHeader">
              <Setter Property="HorizontalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="GroupHeaderStyle" TargetType="{x:Type GroupItem}">
              <Setter Property="Margin" Value="0,2,0,4"/>
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type GroupItem}">
                    <Expander x:Name="exp"
                              IsExpanded="True"
                              Background="LightGray"
                              BorderBrush="Gray"
                              BorderThickness="1"
                              Foreground="Black"
                              Padding="2"
                              Margin="2,0">
                      <Expander.Header>
                        <DockPanel>
                          <TextBlock FontWeight="Bold" Text="{Binding Path=Name}" Margin="5,0,0,0" />
                          <TextBlock Text=" (" FontWeight="Bold" />
                          <TextBlock FontWeight="Bold" Text="{Binding Path=ItemCount}"/>
                          <TextBlock Text=")" FontWeight="Bold" />
                        </DockPanel>
                      </Expander.Header>
                      <ItemsPresenter />
                    </Expander>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </DataGrid.Resources>

          <DataGrid.GroupStyle>
            <GroupStyle ContainerStyle="{StaticResource GroupHeaderStyle}">
              <GroupStyle.Panel>
                <ItemsPanelTemplate>
                  <DataGridRowsPresenter />
                </ItemsPanelTemplate>
              </GroupStyle.Panel>
            </GroupStyle>
          </DataGrid.GroupStyle>

          <DataGrid.Columns>
            <DataGridTextColumn Header="Order"
                                IsReadOnly="True"
                                Width="40"
                                Visibility="Collapsed"
                                Binding="{Binding DOrder}" />

            <DataGridTemplateColumn Header="Question"
                                    IsReadOnly="True"
                                    Width="400"
                                    HeaderStyle="{StaticResource CenterCol}">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding QuestionText, Mode=TwoWay}"
                             TextAlignment="Left"
                             TextWrapping="Wrap"
                             Padding="4,0"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="FE Answer"
                                    IsReadOnly="True"
                                    Width="Auto"
                                    MinWidth="90"
                                    MaxWidth="200">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding LUP_AnswerText, Mode=TwoWay}"
                             TextAlignment="Left"
                             VerticalAlignment="Center"
                             TextWrapping="Wrap"
                             Padding="2,0"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="FE Notes"
                                    IsReadOnly="True"
                                    Width="Auto"
                                    MinWidth="90"
                                    MaxWidth="200">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding FENotes, Mode=TwoWay}"
                             TextAlignment="Left"
                             VerticalAlignment="Center"
                             TextWrapping="Wrap"
                             Padding="2,0"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="Risk Comment"
                                    IsReadOnly="True"
                                    Width="Auto"
                                    MinWidth="200"
                                    MaxWidth="600">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding LUP_EmailComment, Mode=TwoWay}"
                             TextAlignment="Left"
                             VerticalAlignment="Center"
                             TextWrapping="Wrap"
                             Padding="2,0"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="Score"
                                IsReadOnly="True"
                                Width="40" >
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Score, Mode=TwoWay}"
                             TextAlignment="Right"
                             VerticalAlignment="Center"
                             TextWrapping="Wrap"
                             Padding="2,0"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTextColumn Header="QGroup"
                                IsReadOnly="True"
                                Width="40"
                                Visibility="Collapsed"
                                Binding="{Binding QGroup}" />
          </DataGrid.Columns>
        </DataGrid>
      </Grid>
    </Border>
  </Grid>
</Grid>